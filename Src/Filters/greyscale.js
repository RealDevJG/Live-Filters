// class GreyscaleFilter
// {
//     constructor()
//     {

//     }
// }

function GreyscaleFilter(_img)
{
    for (let y = 0; y < _img.height; ++y)
    {
        for (let x = 0; x < _img.width; ++x)
        {
            // TODO: greyscale image here. try set it first without needing to return it because js might do it by reference instead of value
        }
    }
}
